---
---

Internal: ESLint plugin `@impactx/eslint-plugin-ui` agora roda em `error` em vez de `warn`. CI bloqueia novos drifts de Tailwind cru e duplicacao de componente. Sem mudancas no package publicavel.
