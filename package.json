{
  "name": "impactx-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Impact X UI — multi-DS, multi-theme component library distributed via @impactx/ui CLI",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "registry:build": "node scripts/build-registry.mjs"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
